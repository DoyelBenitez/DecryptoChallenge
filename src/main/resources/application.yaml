# application.yaml
server:
  servlet:
    context-path: '/challenge/api/'

spring:
  application:
    name: 'challenge'

security:
  jwt:
    token:
      expire-in-milliseconds: 3600000

# Log Configuration
logging:
  level:
    org:
      springframework:
        # web: 'DEBUG'
        web.filter.CommonsRequestLoggingFilter: 'DEBUG'
        # hibernate: 'DEBUG'
      SQL: 'DEBUG'
      apache:
        coyote:
          http11: 'DEBUG'  # Nivel de log para HTTP11
      # type: 'TRACE'
      tool:
      # schema: 'TRACE'